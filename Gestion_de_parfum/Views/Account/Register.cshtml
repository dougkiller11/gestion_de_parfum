@model Gestion_de_parfum.Models.RegisterViewModel

@{
    ViewData["Title"] = "Inscription";
}

<div class="login-container">
    <div class="login-wrapper">
        <div class="login-card">
            <div class="login-header">
                <div class="perfume-icon">🧴</div>
                <h1>Créer un compte</h1>
                <p class="subtitle">Inscrivez-vous pour accéder à la gestion de parfums</p>
            </div>

            @if (ViewData.ModelState.ErrorCount > 0)
            {
                <div class="alert-error">
                    <span class="error-icon">⚠️</span>
                    <span>Certains champs sont invalides. Merci de vérifier.</span>
                </div>
            }

            <form asp-action="Register" method="post" class="login-form">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="ReturnUrl" />

                <div class="form-group">
                    <label class="form-label" asp-for="Nom">
                        <span class="label-icon">👤</span>
                        Nom complet
                    </label>
                    <input class="form-input" asp-for="Nom" placeholder="Votre nom complet" />
                    <span class="validation-message" asp-validation-for="Nom"></span>
                </div>

                <div class="form-group">
                    <label class="form-label" asp-for="Email">
                        <span class="label-icon">📧</span>
                        Email
                    </label>
                    <input class="form-input" asp-for="Email" placeholder="Votre email" />
                    <span class="validation-message" asp-validation-for="Email"></span>
                </div>

                <div class="form-group">
                    <label class="form-label" asp-for="Password">
                        <span class="label-icon">🔒</span>
                        Mot de passe
                    </label>
                    <input class="form-input" asp-for="Password" placeholder="Choisissez un mot de passe" />
                    <span class="validation-message" asp-validation-for="Password"></span>
                </div>

                <div class="form-group">
                    <label class="form-label" asp-for="ConfirmPassword">
                        <span class="label-icon">🔒</span>
                        Confirmer le mot de passe
                    </label>
                    <input class="form-input" asp-for="ConfirmPassword" placeholder="Répétez le mot de passe" />
                    <span class="validation-message" asp-validation-for="ConfirmPassword"></span>
                </div>

                <button class="login-button" type="submit">
                    <span>Créer mon compte</span>
                </button>
            </form>

            <div class="register-link">
                <span>Déjà un compte ?</span>
                <a asp-action="Login">Se connecter</a>
            </div>
        </div>
    </div>
</div>

<partial name="_AuthStyles" />


